<section ng-app="Contacts">
	<div ng-controller="UserCtrl">
		<form ng-submit="search()" >
			<input type="text" ng-model="val" ng-change="search()">
			<input type="submit" value="Add">
		</form>
		<ul>
			<li ng-repeat="user in users">  <!-- | ﬁlter:city | orderBy: 'name' -->
				{{user.name}}
			</li>
		</ul>
	</div>
</section>